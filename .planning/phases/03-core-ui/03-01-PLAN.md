---
phase: 03-core-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - components.json
  - src/app/globals.css
  - src/app/layout.tsx
  - src/providers/query-provider.tsx
  - src/providers/query-provider.test.tsx
  - src/app/api/places/resolve/route.ts
  - src/lib/google/places-client.ts
  - src/components/ui/button.tsx
  - src/components/ui/input.tsx
  - src/components/ui/textarea.tsx
  - src/components/ui/card.tsx
  - src/components/ui/badge.tsx
  - src/components/ui/separator.tsx
  - src/components/ui/scroll-area.tsx
  - src/components/ui/tabs.tsx
  - src/components/ui/skeleton.tsx
  - src/components/ui/alert.tsx
  - src/components/ui/tooltip.tsx
  - src/lib/map/day-colors.ts
  - src/types/itinerary.ts
  - src/components/itinerary-view.test.tsx
  - src/components/place-row.test.tsx
  - src/components/map-view.test.tsx
autonomous: false
requirements: [DISP-01, DISP-02, DISP-03, DISP-04, AUTH-01]
user_setup:
  - service: google-maps-platform
    why: "MapView (DISP-03) renders Google Maps in the browser via @vis.gl/react-google-maps"
    env_vars:
      - name: NEXT_PUBLIC_GOOGLE_MAPS_API_KEY
        source: "GCP Console -> APIs & Services -> Credentials -> browser-restricted key (Maps JavaScript API only, HTTP referrer restricted). Must already exist in .env.local per Phase 1 .env.example."

must_haves:
  truths:
    - "Running npx shadcn@latest add succeeds because components.json exists and Tailwind v4 is detected"
    - "Geist Sans/Mono fonts still render after shadcn init (font tokens restored in globals.css)"
    - "buildCityBias('高雄市') returns a circle centered near Kaohsiung (22.6273, 120.3014), not Taiwan center"
    - "QueryProvider wraps the app so any TanStack Query hook has a client ancestor"
    - "The 5-color DAY_COLORS palette and getDayColor(dayIndex) helper exist as a shared module"
  artifacts:
    - "components.json (shadcn config)"
    - "11 shadcn component files under src/components/ui/"
    - "src/providers/query-provider.tsx"
    - "src/lib/map/day-colors.ts — provides DAY_COLORS constant + getDayColor(dayNumber) for plans 03-02/03/04"
    - "src/types/itinerary.ts — provides ResolvedPlace, OptimizeResult re-exports + PlaceCoord for coordinate-gap join in 03-04 (client-side OptimizeResult + enrichment types)"
    - "Three failing Wave-0 test scaffolds: itinerary-view.test.tsx, place-row.test.tsx, map-view.test.tsx"
  key_links:
    - "layout.tsx -> QueryProvider -> {children} (provider tree)"
    - "resolve/route.ts -> buildCityBias() in places-client.ts -> CITY lookup table"
    - "globals.css @theme inline -> --font-sans/--font-mono -> Geist variables from layout.tsx"
---

<objective>
Wave 0 scaffolding for all of Phase 3. Installs the three new packages, initializes shadcn/ui against the existing Tailwind v4 project, adds the 11 shadcn components the UI-SPEC requires, restores the Geist font tokens that shadcn init overwrites, wires QueryProvider + ThemeProvider into layout.tsx, fixes the buildCityBias() bug carried over from Phase 1, and creates the shared day-color palette, client-side itinerary types, and the three failing test scaffolds that downstream plans turn green.

Purpose: Every other Phase 3 plan depends on shadcn components, the day-color palette, the client types, and a working buildCityBias(). This plan exists so no component plan has to stop and scaffold.
Output: components.json, 11 shadcn UI files, query-provider, day-colors module, itinerary types, fixed buildCityBias(), and 3 RED test scaffolds.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ui/03-CONTEXT.md
@.planning/phases/03-core-ui/03-RESEARCH.md
@.planning/phases/03-core-ui/03-UI-SPEC.md
@src/app/globals.css
@src/app/layout.tsx
@src/app/api/places/resolve/route.ts
@src/lib/optimizer/schedule.ts
@src/lib/validation/resolve.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify lucide-react package legitimacy before install</name>
  <files>package.json</files>
  <action>HALT before any `npm install`. Present the lucide-react legitimacy evidence to the human and wait for explicit "approved" before Task 2 runs. This checkpoint is NOT auto-approvable (legitimacy gate for a SUS package per 03-RESEARCH.md Package Legitimacy Audit; T-03-SC).</action>
  <what-built>Nothing yet — this gate runs BEFORE `npm install`. 03-RESEARCH.md Package Legitimacy Audit flagged lucide-react as SUS (publish date 2026-06-18 is "too new" to the checker), though it reports ~84M weekly downloads from the official lucide-icons org. @vis.gl/react-google-maps and next-themes are marked OK.</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/lucide-react in a browser.
    2. Confirm the repository link points to github.com/lucide-icons/lucide (official org).
    3. Confirm weekly downloads are in the tens of millions (not a typosquat with near-zero downloads).
    4. Confirm the latest published version is a 0.x release consistent with the icon library (not a hijacked 9.x or a wildly different package).
  </how-to-verify>
  <verify>
    <human>Confirm lucide-react package is either approved (install proceeds) or rejected (remove from install list). After decision, package.json must reflect the outcome: lucide-react present if approved, absent if rejected.</human>
    <automated>node -e "const p=require('./package.json'); console.log('lucide-react:', p.dependencies['lucide-react'] || p.devDependencies?.['lucide-react'] || 'absent');"</automated>
  </verify>
  <resume-signal>Type "approved" to proceed with install, or describe the discrepancy to halt.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install packages, init shadcn, add 11 components, restore font tokens</name>
  <read_first>
    - src/app/globals.css (current @theme inline block — must preserve --font-sans/--font-mono after init)
    - src/app/layout.tsx (Geist font variable wiring — confirms --font-geist-sans / --font-geist-mono)
    - .planning/phases/03-core-ui/03-RESEARCH.md Pattern 5 + Pitfall 4 (shadcn init on Tailwind v4, font-token overwrite)
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 5 (exact 11-component list)
  </read_first>
  <action>
    Run `npm install @vis.gl/react-google-maps lucide-react next-themes` (versions per 03-RESEARCH.md: @vis.gl/react-google-maps 1.21.0, lucide-react 0.4.6, next-themes 1.8.3 — accept the registry latest within those major lines).
    Run `npx shadcn@latest init` with: style Default, base color Zinc, CSS variables yes. This creates components.json and rewrites globals.css.
    Run `npx shadcn@latest add button input textarea card badge separator scroll-area tabs skeleton alert tooltip` (all 11 components from UI-SPEC section 5).
    After init rewrites globals.css, restore the two Geist font tokens inside the `@theme inline` block (Pitfall 4): add `--font-sans: var(--font-geist-sans);` and `--font-mono: var(--font-geist-mono);`. Do not remove shadcn's added color tokens; only re-add the two font lines if init dropped them.
    Verify the merged globals.css still has `@import "tailwindcss";` at the top and both `--font-sans` and `--font-mono` resolving to the Geist variables.
  </action>
  <verify>
    <automated>test -f components.json && for c in button input textarea card badge separator scroll-area tabs skeleton alert tooltip; do test -f "src/components/ui/$c.tsx" || { echo "MISSING ui/$c.tsx"; exit 1; }; done && grep -q -- '--font-sans' src/app/globals.css && grep -q -- '--font-mono' src/app/globals.css && grep -q '@import "tailwindcss"' src/app/globals.css && echo OK</automated>
  </verify>
  <done>components.json exists; all 11 src/components/ui/*.tsx files exist; globals.css still imports tailwindcss and still defines --font-sans and --font-mono mapping to the Geist variables.</done>
</task>

<task type="auto">
  <name>Task 3: Add QueryProvider + ThemeProvider to layout, create shared day-colors + client itinerary types</name>
  <read_first>
    - src/app/layout.tsx (current RootLayout — provider wiring target)
    - .planning/phases/03-core-ui/03-RESEARCH.md Pattern 4 (QueryProvider singleton) + Code Examples (DAY_COLORS, coordinate-join shape)
    - src/lib/optimizer/schedule.ts (OptimizeResult / ScheduledVisit — the client types must mirror these field-for-field)
    - src/lib/validation/resolve.ts (ResolvedPlace shape — placeId, displayName, formattedAddress, lat, lng)
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 4 Map Day Colors table
  </read_first>
  <action>
    Create `src/providers/query-provider.tsx` as a `'use client'` component exporting `QueryProvider({children})` that wraps children in `QueryClientProvider` with a module-level singleton `QueryClient` (defaultOptions.queries.staleTime = 60000). Per 03-RESEARCH.md Pattern 4.
    Also export a `ThemeProvider` wrapper from `src/providers/theme-provider.tsx` (`'use client'`) re-exporting next-themes ThemeProvider with `attribute="class" defaultTheme="light" enableSystem={false}` (dark-mode UI toggle is deferred to Phase 4 per UI-SPEC section 12 — provider only, no toggle).
    Modify `src/app/layout.tsx`: wrap `{children}` inside the `<body>` with `<ThemeProvider><QueryProvider>{children}</QueryProvider></ThemeProvider>`. Keep `lang="zh-Hant"`, the Geist variable className, and `bg-gray-50` exactly as they are.
    Create `src/lib/map/day-colors.ts` exporting `DAY_COLORS` (the 5 hex values in UI-SPEC section 4 order: #2563EB, #16A34A, #D97706, #9333EA, #DC2626) as a readonly tuple and `getDayColor(dayIndex: number): string` returning `DAY_COLORS[dayIndex % DAY_COLORS.length]`.
    Create `src/types/itinerary.ts` re-exporting `OptimizeResult`, `ScheduledVisit` from `@/lib/optimizer/schedule` and `ResolvedPlace` from `@/lib/validation/resolve`, plus two new client-only types: `PlaceCoord = { lat: number; lng: number }` and `PlaceDetail = { openingHours: unknown; priceLevel: number | null; hoursUnknown: boolean }`. These document the coordinate-gap and details-gap joins that 03-03/03-04 perform.
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f src/providers/query-provider.tsx && test -f src/lib/map/day-colors.ts && test -f src/types/itinerary.ts && grep -q 'QueryProvider' src/app/layout.tsx && grep -q '2563EB' src/lib/map/day-colors.ts && echo OK</automated>
  </verify>
  <done>query-provider.tsx, theme-provider.tsx, day-colors.ts, itinerary.ts all exist; layout.tsx wraps children in both providers; DAY_COLORS holds the 5 UI-SPEC hex values; tsc is clean.</done>
</task>

<task type="auto">
  <name>Task 4: Fix buildCityBias() with Taiwan city lookup table</name>
  <read_first>
    - src/app/api/places/resolve/route.ts (where bias is built from locationBias — confirms current path)
    - src/lib/google/places-client.ts (buildCityBias / LocationBias type — bug location per CONTEXT.md and 03-RESEARCH.md Pattern 6)
    - .planning/phases/03-core-ui/03-RESEARCH.md Pattern 6 (full 24-entry CITY_COORDS table + normalize/prefix-match logic, marked [ASSUMED])
  </read_first>
  <action>
    In `src/lib/google/places-client.ts`, replace the body of `buildCityBias(city)` so it consults a `TAIWAN_CITY_COORDS: Record<string, {lat; lng; radius}>` lookup with the ~24 entries listed in 03-RESEARCH.md Pattern 6 (6 special municipalities with city/縣市 variants + counties: 基隆 新竹 嘉義 宜蘭 花蓮 台東 澎湖 金門). Normalize input via `city.trim()`, try direct lookup, then the 2-char prefix (so "台北市" falls back to "台北"). On a hit, return `{ circle: { center: {latitude, longitude}, radius } }`. On a miss, return the existing Taiwan-center fallback but with radius 100000 (so non-Taiwan cities like "京都市" still get a broad bias while textQuery disambiguates).
    If `buildCityBias()` does not currently exist in places-client.ts (it may live inline in route.ts), create it in places-client.ts and have resolve/route.ts call it for the text path when no explicit locationBias was supplied. Do not change the explicit-locationBias override path.
    The coordinate values are [ASSUMED] from public sources — keep the inline comment marking them as such.
  </action>
  <verify>
    <automated>npx tsc --noEmit && grep -q 'TAIWAN_CITY_COORDS' src/lib/google/places-client.ts && grep -q '22.6273' src/lib/google/places-client.ts && grep -q '100000' src/lib/google/places-client.ts && echo OK</automated>
  </verify>
  <done>buildCityBias('高雄市') resolves via prefix to the Kaohsiung entry (22.6273, 120.3014); buildCityBias('京都市') returns the 100km Taiwan-center fallback; tsc clean; resolve route still compiles.</done>
</task>

<task type="auto">
  <name>Task 5: Create the three failing Wave-0 test scaffolds + query-provider test</name>
  <read_first>
    - src/app/api/optimize/route.test.ts (existing Vitest + vi.mock patterns in this repo)
    - .planning/phases/03-core-ui/03-RESEARCH.md Validation Architecture (Wave 0 Gaps list + DISP test map + vi.mock('@vis.gl/react-google-maps') note)
    - src/lib/optimizer/schedule.ts (OptimizeResult shape the itinerary tests assert against)
  </read_first>
  <action>
    Create four test files that fail today because their components do not exist yet (RED scaffolds that 03-02/03-03/03-04 turn green):
    - `src/components/itinerary-view.test.tsx` — imports `ItineraryView` from `./itinerary-view`, renders an OptimizeResult fixture with 2 days, asserts day headings "第 1 天"/"第 2 天" and each visit displayName + time slot appear (DISP-01).
    - `src/components/place-row.test.tsx` — imports `PlaceRow` from `./place-row`, asserts a row with hoursUnknown=true renders the amber warning text "營業時間未知，建議出發前確認", and a row with priceLevel renders the price badge (DISP-02).
    - `src/components/map-view.test.tsx` — `vi.mock('@vis.gl/react-google-maps', ...)` returning lightweight stubs (Map, AdvancedMarker, Polyline, APIProvider, InfoWindow, useMap, useMapsLibrary, useAdvancedMarkerRef), imports `MapView` from `./map-view`, asserts one Polyline stub per day and one marker stub per visit (DISP-03).
    - `src/providers/query-provider.test.tsx` — asserts `QueryProvider` renders its children (this one passes now since QueryProvider exists from Task 3).
    Each component import will fail to resolve until its plan implements it — that is the intended RED state. Use a `// TODO(03-NN): green when <component> lands` comment in each.
    Do NOT run these in the per-task gate as pass-required; mark the three component tests as the RED gate documented in the SUMMARY.
  </action>
  <verify>
    <automated>test -f src/components/itinerary-view.test.tsx && test -f src/components/place-row.test.tsx && test -f src/components/map-view.test.tsx && test -f src/providers/query-provider.test.tsx && npx vitest run src/providers/query-provider.test.tsx && echo OK</automated>
  </verify>
  <done>All four test files exist; query-provider.test.tsx passes; the three component test files fail only because their target components are not yet implemented (no syntax/import-typo errors).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> dev machine | Three new packages installed; supply-chain risk |
| browser -> Maps JS API | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY is exposed in the client bundle by design |
| user city input -> resolve route | City string drives buildCityBias() lookup |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-SC | Tampering | npm install of @vis.gl/react-google-maps, lucide-react, next-themes | high | mitigate | Task 1 blocking-human checkpoint verifies lucide-react (SUS) on npmjs.com before install; @vis.gl + next-themes marked OK in audit |
| T-03-01 | Information Disclosure | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY in client bundle | medium | accept | Key is intentionally public for Maps JS; protection is GCP HTTP-referrer + Maps-JS-only scope (operator task, Phase 1 cost-controls) |
| T-03-02 | Tampering | buildCityBias() city lookup | low | mitigate | Lookup is a fixed Record keyed by normalized string; unknown keys fall through to a constant fallback — no dynamic property execution, no prototype pollution |
</threat_model>

<verification>
- `npx tsc --noEmit` clean after all tasks
- `npx vitest run` — 161 existing tests still green; query-provider.test.tsx green; the 3 component tests RED (import-unresolved only)
- components.json present; 11 ui/*.tsx present; globals.css retains tailwindcss import + Geist font tokens
- buildCityBias() returns city-specific coords for known cities, 100km fallback for unknown
</verification>

<success_criteria>
- shadcn initialized and 11 components installed
- Geist fonts preserved after init
- QueryProvider + ThemeProvider in layout.tsx
- buildCityBias() city-aware with 100km fallback
- DAY_COLORS palette + client itinerary types shared
- 3 RED component test scaffolds + 1 green provider test
</success_criteria>

## Artifacts this phase produces (this plan)

- `components.json`, `src/components/ui/{button,input,textarea,card,badge,separator,scroll-area,tabs,skeleton,alert,tooltip}.tsx`
- `src/providers/query-provider.tsx`, `src/providers/theme-provider.tsx`
- `src/lib/map/day-colors.ts`, `src/types/itinerary.ts`
- Patched `src/app/globals.css`, `src/app/layout.tsx`, `src/lib/google/places-client.ts`
- `src/components/{itinerary-view,place-row,map-view}.test.tsx`, `src/providers/query-provider.test.tsx`

<output>
Create `.planning/phases/03-core-ui/03-01-SUMMARY.md` when done
</output>
