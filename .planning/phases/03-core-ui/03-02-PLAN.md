---
phase: 03-core-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/page.tsx
  - src/components/place-input-panel.tsx
  - src/components/place-input-panel.test.tsx
  - src/components/resolved-place-list.tsx
  - src/components/itinerary-view.tsx
  - src/components/day-card.tsx
  - src/components/results-layout.tsx
  - src/components/itinerary-view.test.tsx
autonomous: true
requirements: [DISP-01, AUTH-01]

must_haves:
  truths:
    - "An anonymous user pastes Chinese place names + a city, clicks 查詢地點, and sees each resolved place with its address — no login"
    - "From the confirmed list the user clicks 最佳化行程 and receives a day-by-day itinerary rendered as 第 N 天 cards with ordered place rows showing arrival–departure times"
    - "The full input -> resolve -> optimize -> view flow runs with zero auth checks (AUTH-01)"
    - "itinerary-view.test.tsx (DISP-01 scaffold from 03-01) now passes"
  artifacts:
    - "src/components/place-input-panel.tsx (3-step flow controller, replaces PlaceResolverForm)"
    - "src/components/resolved-place-list.tsx"
    - "src/components/itinerary-view.tsx + src/components/day-card.tsx"
    - "src/components/results-layout.tsx (stacked container; map slot filled by 03-04)"
    - "src/app/page.tsx rendering PlaceInputPanel"
    - "src/components/place-input-panel.test.tsx (E2E happy-path)"
  key_links:
    - "page.tsx -> PlaceInputPanel -> POST /api/places/resolve -> resolvedPlaces state"
    - "PlaceInputPanel -> POST /api/optimize (placeIds) -> OptimizeResult -> ResultsLayout -> ItineraryView -> DayCard"
    - "PlaceInputPanel keeps resolvedPlaces (with lat/lng) in state for 03-04 coordinate join"
---

<objective>
The Wave 1 walking slice of the Phase 3 UI: the thinnest complete vertical path from pasting places to seeing a day-by-day itinerary, with no account. This is the MVP backbone — after this plan an anonymous user can do the whole core job (input -> resolve -> optimize -> view), even though the itinerary rows are minimal (names + times only) and the map is a placeholder. Plans 03-03 (rich rows, warnings, unscheduled, progress) and 03-04 (map) enrich this skeleton without restructuring it.

Purpose: Prove the end-to-end flow works against the real Phase 1 + Phase 2 Route Handlers before adding display polish. Locks the prop contracts (ItineraryView props, ResultsLayout slots, resolvedPlaces state shape) that 03-03 and 03-04 build on in parallel.
Output: PlaceInputPanel, ResolvedPlaceList, ItineraryView, DayCard, ResultsLayout, rewired page.tsx, and a passing E2E happy-path test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ui/03-CONTEXT.md
@.planning/phases/03-core-ui/03-UI-SPEC.md
@.planning/phases/03-core-ui/03-RESEARCH.md
@src/components/place-resolver-form.tsx
@src/lib/validation/resolve.ts
@src/lib/validation/optimize.ts
@src/lib/optimizer/schedule.ts
@src/types/itinerary.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: E2E happy-path test for input -> resolve -> optimize -> itinerary (RED first)</name>
  <read_first>
    - src/app/api/optimize/route.test.ts (fetch-mock + vi.mock patterns used in this repo)
    - src/lib/validation/resolve.ts (ResolvedPlace fields the resolve fetch returns)
    - src/lib/optimizer/schedule.ts (OptimizeResult fields the optimize fetch returns)
    - .planning/phases/03-core-ui/03-UI-SPEC.md sections 7 (Flow A/B/D) and 8 (exact zh-TW copy)
  </read_first>
  <behavior>
    - Render PlaceInputPanel. Mock global fetch: POST /api/places/resolve returns a 2-place ResolvedPlace[] array (with placeId/displayName/formattedAddress/lat/lng); POST /api/optimize returns an OptimizeResult with 1 day, 2 visits.
    - Type a city ("台北市") and two newline-separated names, click 查詢地點: both displayNames + formattedAddresses appear (confirmation step).
    - Click 最佳化行程: the day heading "第 1 天" and both visits' scheduledStart–scheduledEnd time slots appear.
    - No Supabase/auth module is imported or called anywhere in the rendered tree (AUTH-01) — assert fetch was called only with /api/places/resolve and /api/optimize.
  </behavior>
  <action>
    Write `src/components/place-input-panel.test.tsx` using @testing-library/react + Vitest. Mock `global.fetch` to branch on URL. This is the RED test — it fails until Task 2/3 implement the components. Commit it before the implementation commit. Use the exact CTA strings from UI-SPEC section 8 (查詢地點 / 最佳化行程) in the queries so the test pins the copy contract.
  </action>
  <verify>
    <automated>test -f src/components/place-input-panel.test.tsx && grep -q '最佳化行程' src/components/place-input-panel.test.tsx && grep -q '第 1 天' src/components/place-input-panel.test.tsx && echo OK</automated>
  </verify>
  <done>E2E test file exists, references the real CTA copy and the 第 N 天 heading, and fails (RED) because PlaceInputPanel/ItineraryView are not implemented yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PlaceInputPanel + ResolvedPlaceList — input, resolve, confirm</name>
  <read_first>
    - src/components/place-resolver-form.tsx (existing resolve fetch + parsing logic to port and replace)
    - src/lib/validation/resolve.ts (ResolvedPlace; resolve returns ResolvedPlace[] OR per-input {original_query,status:"NOT_FOUND"} markers — filter those out for the list)
    - src/components/ui/{button,input,textarea,card,alert,skeleton}.tsx (shadcn components from 03-01)
    - .planning/phases/03-core-ui/03-UI-SPEC.md sections 7 (Flow A/B) and 8 (all copy: placeholders, helpers, errors, confirmation callout)
    - src/types/itinerary.ts (ResolvedPlace re-export + state types)
  </read_first>
  <action>
    Create `src/components/place-input-panel.tsx` (`'use client'`) as the top-level flow controller holding state: `rawInputs: string`, `city: string`, `resolvedPlaces: ResolvedPlace[]`, `optimizeResult: OptimizeResult | null`, `loading: 'idle'|'resolving'|'optimizing'`, `error: string | null`. Replace `place-resolver-form.tsx` (delete it or stop importing it).
    Implement Flow A (input): a shadcn `Input` for 目的地城市 (helper "必填，用於縮小地點搜尋範圍") and a `Textarea` for 地點清單 (placeholder and helper exactly per UI-SPEC section 8). The 查詢地點 `Button` (variant default) is disabled when either field is empty; on click split the textarea by newline (trim + drop empties), validate non-empty city (error "請輸入目的地城市") and non-empty list (error "請輸入至少一個地點名稱"), then POST `{inputs, city}` to `/api/places/resolve`. While loading show `Skeleton` rows and label 查詢中.... On failure show an `Alert variant="destructive"` with "地點查詢失敗，請稍後再試" (or "網路錯誤，請稍後再試" on a thrown fetch). On an empty/all-NOT_FOUND result show "沒有找到符合的地點，請檢查輸入內容或城市名稱".
    Create `src/components/resolved-place-list.tsx` (`'use client'`) implementing Flow B: each ResolvedPlace as a shadcn `Card` row with 1-based index, displayName (font-semibold), formattedAddress (text-sm text-muted-foreground), and a ghost-icon remove Button (lucide `X`, aria-label="移除") that removes that place from parent state only (no API call), with `transition-opacity duration-150`. Above the list render an `Alert` (blue-50/blue-700) with the confirmation copy "已解析 {N} 個地點，請確認地點資訊是否正確，然後點擊「最佳化行程」".
    Filter out `{status:"NOT_FOUND"}` markers before storing resolvedPlaces; keep lat/lng on each stored place (needed by 03-04).
    No auth/session imports anywhere (AUTH-01).
  </action>
  <verify>
    <automated>npx tsc --noEmit && test -f src/components/place-input-panel.tsx && test -f src/components/resolved-place-list.tsx && grep -q '查詢地點' src/components/place-input-panel.tsx && grep -q '已解析' src/components/resolved-place-list.tsx && grep -Lq 'supabase' src/components/place-input-panel.tsx && echo OK</automated>
  </verify>
  <done>PlaceInputPanel resolves places against /api/places/resolve and renders ResolvedPlaceList with remove buttons; all copy matches UI-SPEC section 8; no auth imports; tsc clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: OptimizeButton + ItineraryView + DayCard + ResultsLayout + page wiring (turns RED tests GREEN)</name>
  <read_first>
    - src/lib/validation/optimize.ts (optimizeRequestSchema — body is {placeIds, numDays?, travelDate?}; placeIds max 25)
    - src/lib/optimizer/schedule.ts (OptimizeResult: suggestedDays, days[].{dayNumber,visits[]}, unscheduled[]; ScheduledVisit fields)
    - src/components/itinerary-view.test.tsx and src/components/place-input-panel.test.tsx (the RED tests this task must turn green)
    - src/app/page.tsx (current entry — replace PlaceResolverForm with PlaceInputPanel)
    - src/components/ui/{card,separator,scroll-area,button}.tsx
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 7 Flow C/D + section 6 (layout) + section 8 (第 N 天 / 共 M 個地點 / 搭車 N 分)
    - src/lib/map/day-colors.ts (getDayColor for the day-color dot)
  </read_first>
  <action>
    In `place-input-panel.tsx` add the OptimizeButton: a 最佳化行程 `Button` disabled until resolvedPlaces.length >= 1; on click POST `{ placeIds: resolvedPlaces.map(p=>p.placeId) }` to `/api/optimize`, set loading 'optimizing' (label 最佳化中...), store the OptimizeResult, and on failure show Alert "行程最佳化失敗，請重新嘗試" with a 重試 button. After a successful optimize, render `ResultsLayout` and stop showing the input panel's empty-state.
    Create `src/components/itinerary-view.tsx` (`'use client'`): props `{ itinerary: OptimizeResult; coordHoursById?: ... }` — for THIS plan accept `{ itinerary: OptimizeResult }` and map `itinerary.days` to `DayCard`. (Leave a clearly-typed optional prop slot that 03-03 will use to pass per-place details; do not block on it.)
    Create `src/components/day-card.tsx` (`'use client'`): a shadcn `Card`; header "第 {dayNumber} 天" (text-lg font-semibold) + summary "共 {visits.length} 個地點"; body is an ordered list of minimal rows — each row shows a day-color dot via getDayColor(dayNumber-1), the 1-based index, displayName (font-semibold text-base), and the time slot "{scheduledStart} – {scheduledEnd}" (text-sm text-muted-foreground), separated by shadcn `Separator`. The rich PlaceRow (hours/price/warnings) is added by 03-03; keep DayCard rendering inline minimal rows now so DISP-01 is fully satisfied by this plan.
    Create `src/components/results-layout.tsx` (`'use client'`): a container that renders `ItineraryView` and reserves a map slot. For this plan render a single stacked column (ItineraryView, then a placeholder `<div>` with text "地圖即將顯示"); 03-04 replaces the placeholder with the responsive Tabs/flex layout + MapView. Pass `itinerary` through; also accept and forward `resolvedPlaces` so 03-04 can build the coordinate map without changing this signature.
    Rewrite `src/app/page.tsx` to render `<main class="max-w-7xl mx-auto px-4 py-8">` containing `<PlaceInputPanel />` (server component renders the client panel; no auth).
    Ensure `src/components/itinerary-view.test.tsx` and `src/components/place-input-panel.test.tsx` both pass.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/itinerary-view.test.tsx src/components/place-input-panel.test.tsx && grep -q 'PlaceInputPanel' src/app/page.tsx && grep -q '第' src/components/day-card.tsx && echo OK</automated>
  </verify>
  <done>Clicking 最佳化行程 posts placeIds to /api/optimize and renders 第 N 天 DayCards with ordered name+time rows; itinerary-view.test.tsx and place-input-panel.test.tsx pass; page.tsx renders PlaceInputPanel; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/places/resolve | User textarea/city input crosses to the server route (already Zod-validated server-side) |
| browser -> /api/optimize | resolvedPlaces placeIds crosses to the server route (Zod max-25 cap server-side) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-03 | Tampering | place list parsing (textarea -> string[]) | low | mitigate | Split by newline + trim + filter; never JSON.parse user input on the client (03-RESEARCH.md Security Domain) |
| T-03-04 | Information Disclosure | displayName rendered in itinerary rows | low | mitigate | React JSX auto-escapes; no dangerouslySetInnerHTML for place names |
| T-03-05 | Elevation of Privilege | anonymous flow calling resolve/optimize | low | accept | AUTH-01 explicitly permits anonymous use; routes intentionally have no auth gate in Phase 3 |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run` — full suite green including itinerary-view.test.tsx + place-input-panel.test.tsx; map-view.test.tsx still RED (03-04)
- page.tsx renders PlaceInputPanel; no Supabase/auth import in any Phase 3 component
- Manual: at desktop width the full flow input->resolve->optimize->第N天 cards works end to end
</verification>

<success_criteria>
- Anonymous end-to-end flow produces a day-by-day itinerary (DISP-01, AUTH-01)
- All zh-TW copy from UI-SPEC section 8 used (no placeholder text)
- resolvedPlaces state retains lat/lng for the downstream map join
- E2E + DISP-01 tests green
</success_criteria>

## Artifacts this phase produces (this plan)

- `src/components/place-input-panel.tsx` (+ `.test.tsx`)
- `src/components/resolved-place-list.tsx`
- `src/components/itinerary-view.tsx`, `src/components/day-card.tsx`
- `src/components/results-layout.tsx`
- Rewritten `src/app/page.tsx`
- `src/components/place-resolver-form.tsx` removed/retired

<output>
Create `.planning/phases/03-core-ui/03-02-SUMMARY.md` when done
</output>
