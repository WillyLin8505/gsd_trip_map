---
phase: 03-core-ui
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/components/place-row.tsx
  - src/components/place-row.test.tsx
  - src/components/day-card.tsx
  - src/components/itinerary-view.tsx
  - src/components/unscheduled-alert.tsx
  - src/components/progress-steps.tsx
  - src/components/place-input-panel.tsx
  - src/lib/places/use-place-details.ts
autonomous: true
requirements: [DISP-02, DISP-01, AUTH-01]

must_haves:
  truths:
    - "Each itinerary place row shows its opening hours and price level fetched from GET /api/places/details (DISP-02)"
    - "A place with hoursUnknown=true shows the amber badge 營業時間未知，建議出發前確認 in the itinerary row"
    - "Places that /api/optimize could not schedule appear in an UnscheduledAlert with their reasons — never silently dropped"
    - "A 3-step ProgressSteps indicator reflects 輸入地點 -> 確認地點 -> 查看行程 and shows optimize loading labels"
    - "place-row.test.tsx (DISP-02 scaffold from 03-01) now passes"
  artifacts:
    - "src/components/place-row.tsx (rich row: time, travel, hours, price, hoursUnknown badge)"
    - "src/lib/places/use-place-details.ts (fetch + cache details by placeId)"
    - "src/components/unscheduled-alert.tsx"
    - "src/components/progress-steps.tsx"
  key_links:
    - "DayCard -> PlaceRow -> placeDetails(placeId) from use-place-details -> openingHours/priceLevel/hoursUnknown"
    - "PlaceInputPanel -> ProgressSteps (current step) + UnscheduledAlert (optimizeResult.unscheduled)"
    - "GET /api/places/details?placeId=X -> {openingHours, priceLevel, hoursUnknown, cached}"
---

<objective>
Enrich the Wave 1 itinerary skeleton (03-02) with the display data DISP-02 requires. The key insight from the API contracts: POST /api/optimize returns NO opening hours or price — those live only behind GET /api/places/details (cache-first, Phase 1). This plan adds the details-fetch join (analogous to the coordinate-gap join), the rich PlaceRow that renders hours/ticket/travel-time, the hoursUnknown amber badge, the UnscheduledAlert so non-schedulable places are surfaced, and the 3-step ProgressSteps indicator.

Purpose: Satisfy DISP-02 (opening hours + ticket info per place) and complete the itinerary-side UI contract. Runs in parallel with 03-04 (map) because it touches only itinerary/input files, not map files.
Output: PlaceRow, use-place-details hook, UnscheduledAlert, ProgressSteps, and DayCard/ItineraryView/PlaceInputPanel wired to use them.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ui/03-CONTEXT.md
@.planning/phases/03-core-ui/03-UI-SPEC.md
@.planning/phases/03-core-ui/03-RESEARCH.md
@src/app/api/places/details/route.ts
@src/lib/optimizer/schedule.ts
@src/types/itinerary.ts
@src/components/itinerary-view.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: use-place-details hook + rich PlaceRow (turns place-row.test.tsx GREEN)</name>
  <read_first>
    - src/app/api/places/details/route.ts (response: placeId, displayName, openingHours, priceLevel, rating, types, hoursUnknown, cached; query param is ?placeId=)
    - src/components/place-row.test.tsx (the RED DISP-02 test: asserts hoursUnknown amber text + price badge)
    - src/lib/optimizer/schedule.ts (ScheduledVisit: placeId, displayName, scheduledStart, scheduledEnd, travelFromPrevMinutes, waitMinutes, hoursUnknown, warning — NO hours/price)
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 7 Flow D + section 8 (搭車 N 分, badge copy) + section 4 (amber token bg-amber-50 text-amber-700 border-amber-200)
    - src/components/ui/{badge,separator}.tsx, src/lib/map/day-colors.ts
  </read_first>
  <behavior>
    - PlaceRow given a visit with hoursUnknown=true renders the amber badge text "營業時間未知，建議出發前確認" with a lucide Clock icon and classes bg-amber-50 text-amber-700 border-amber-200.
    - PlaceRow given details with priceLevel renders a price Badge ($/$$/$$$/$$$$ by level).
    - PlaceRow given details with openingHours renders a one-line hours summary; given hoursUnknown it shows 營業時間未知 instead.
    - PlaceRow renders travelFromPrevMinutes>0 as "搭車 {N} 分" with a lucide Car icon; first stop (0) omits it.
  </behavior>
  <action>
    Create `src/lib/places/use-place-details.ts`: a client hook `usePlaceDetails(placeIds: string[])` that GETs `/api/places/details?placeId=X` for each unique id, dedupes in-flight requests, and returns `Map<placeId, {openingHours, priceLevel, hoursUnknown}>` plus a loading flag. Tolerate per-id failures (omit that id from the map; the row falls back to optimize-result hoursUnknown). Do not fetch the same id twice.
    Create `src/components/place-row.tsx` (`'use client'`) implementing UI-SPEC Flow D row 2: day-color dot (getDayColor), within-day index, displayName (font-semibold text-base), time slot "{start} – {end}" (text-sm text-muted-foreground), travel "搭車 {N} 分" with lucide `Car` when travelFromPrevMinutes>0, a collapsed opening-hours one-liner from details (or 營業時間未知), and a price `Badge variant="outline"` ($/$$/$$$/$$$$) when priceLevel is present.
    hoursUnknown rule: if the visit OR the details report hoursUnknown, render the amber warning `Badge` with lucide `Clock` and text "營業時間未知，建議出發前確認" using the amber classes from UI-SPEC section 4. (Note: this same warning is mirrored into the map InfoWindow by 03-04 — keep the literal copy identical so both surfaces match the SC4 requirement.)
    Turn `place-row.test.tsx` green.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/place-row.test.tsx && grep -q '營業時間未知，建議出發前確認' src/components/place-row.tsx && grep -q 'amber' src/components/place-row.tsx && test -f src/lib/places/use-place-details.ts && echo OK</automated>
  </verify>
  <done>PlaceRow renders hours/price/travel and the amber hoursUnknown badge; use-place-details fetches details by placeId without duplicate requests; place-row.test.tsx passes; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Wire PlaceRow into DayCard/ItineraryView; add UnscheduledAlert + ProgressSteps</name>
  <read_first>
    - src/components/day-card.tsx (the minimal inline rows from 03-02 — replace with PlaceRow)
    - src/components/itinerary-view.tsx (03-02 prop slot for per-place details)
    - src/components/place-input-panel.tsx (03-02 flow controller — add ProgressSteps + UnscheduledAlert + details fetch trigger)
    - src/lib/optimizer/schedule.ts (unscheduled[].{placeId, reason})
    - src/components/ui/alert.tsx
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 7 Flow C (progress step labels) + Flow D (UnscheduledAlert) + section 8 (以下地點無法排入行程, step labels, 所有地點的營業時間未知...)
  </read_first>
  <action>
    Modify DayCard (src/components/itinerary/day-card.tsx) to replace its inline minimal row rendering with `<PlaceRow>` components. DayCard passes: visit (ScheduledVisit), dayNumber, orderIndex (within-day index), and the per-place PlaceDetail entry from the details Map to each PlaceRow instance. Update `ItineraryView` to accept the details Map prop (the optional slot reserved in 03-02) and thread it to each DayCard.
    In `place-input-panel.tsx`: after a successful optimize, call `usePlaceDetails` for all scheduled placeIds and pass the resulting Map into `ResultsLayout`/`ItineraryView`. Render `UnscheduledAlert` (new file `src/components/unscheduled-alert.tsx`) below the itinerary when `optimizeResult.unscheduled.length > 0`: an `Alert variant="destructive"` titled "以下地點無法排入行程" with a bulleted list of each unscheduled place's displayName (look up via resolvedPlaces) + reason. Never hide unscheduled places.
    Add a global hoursUnknown alert: when every scheduled visit has hoursUnknown=true, render an `Alert` above the DayCards with "所有地點的營業時間未知，建議出發前逐一確認" (UI-SPEC States table).
    Create `src/components/progress-steps.tsx` (`'use client'`): a 3-step indicator (輸入地點 -> 確認地點 -> 查看行程) driven by a `current: 1|2|3` prop, with the active step in blue-600 and `aria-live="polite"` on the container. During optimize loading, show the sequential labels from UI-SPEC Flow C (取得地點詳細資訊... / 計算行車時間... / 最佳化行程順序...). Render ProgressSteps at the top of PlaceInputPanel; drive `current` from the flow state (idle/resolved -> 1/2, optimizeResult present -> 3).
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && test -f src/components/unscheduled-alert.tsx && test -f src/components/progress-steps.tsx && grep -q '以下地點無法排入行程' src/components/unscheduled-alert.tsx && grep -q '查看行程' src/components/progress-steps.tsx && grep -q 'PlaceRow' src/components/day-card.tsx && echo OK</automated>
  </verify>
  <done>DayCard renders rich PlaceRows with details; UnscheduledAlert lists every unscheduled place; ProgressSteps shows 3 steps with correct active state; global hoursUnknown alert appears when all visits are unknown; full vitest suite green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> /api/places/details | placeId query param crosses to the server (validated server-side; cache-first) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-06 | Information Disclosure | openingHours/priceLevel rendered in rows | low | accept | Data is public place metadata from Google Places; no user-private data involved |
| T-03-07 | Tampering | placeId used in details fetch URL | low | mitigate | placeId originates from the resolve response (server-issued), encoded with encodeURIComponent in the query string |
| T-03-08 | Denial of Service | per-place details fetch fan-out | low | mitigate | use-place-details dedupes by placeId and fetches each unique id once; details route is cache-first (30-day TTL) so most calls hit the DB not Google |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run` — full suite green including place-row.test.tsx
- hoursUnknown amber badge copy in PlaceRow matches the map InfoWindow copy used by 03-04 (SC4 both-surfaces requirement)
- UnscheduledAlert renders for non-empty unscheduled; ProgressSteps reflects flow state
</verification>

<success_criteria>
- DISP-02: every place row shows opening hours + price from /api/places/details
- hoursUnknown amber warning visible in the itinerary row (mirrored in map by 03-04)
- Unscheduled places always surfaced (never silently hidden)
- All zh-TW copy from UI-SPEC sections 7-8 used
</success_criteria>

## Artifacts this phase produces (this plan)

- `src/components/place-row.tsx` (+ green `place-row.test.tsx`)
- `src/lib/places/use-place-details.ts`
- `src/components/unscheduled-alert.tsx`, `src/components/progress-steps.tsx`
- Updated `src/components/day-card.tsx`, `src/components/itinerary-view.tsx`, `src/components/place-input-panel.tsx`

<output>
Create `.planning/phases/03-core-ui/03-03-SUMMARY.md` when done
</output>
