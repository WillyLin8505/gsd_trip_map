---
phase: 03-core-ui
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/components/map-view.tsx
  - src/components/map-view-wrapper.tsx
  - src/components/map-view.test.tsx
  - src/components/results-layout.tsx
  - src/components/place-input-panel.tsx
autonomous: true
requirements: [DISP-03, DISP-04, AUTH-01]

must_haves:
  truths:
    - "The map renders one colored polyline per day and numbered markers per visit, each day in its own color (DISP-03)"
    - "Clicking a marker opens an InfoWindow showing the place name, time slot, and (when hoursUnknown) the amber warning 營業時間未知，建議出發前確認 (SC4 map side)"
    - "Marker coordinates are joined from resolvedPlaces by placeId, because /api/optimize returns no lat/lng (coordinate gap)"
    - "At 375px the full flow has no horizontal scroll; itinerary/map use a Tabs toggle on mobile and a side-by-side flex split on desktop (DISP-04)"
    - "map-view.test.tsx (DISP-03 scaffold from 03-01) now passes"
  artifacts:
    - "src/components/map-view.tsx ('use client', APIProvider + Map + per-day Polyline + numbered AdvancedMarker + InfoWindow + DayLegend)"
    - "src/components/map-view-wrapper.tsx (dynamic import with ssr:false)"
    - "responsive src/components/results-layout.tsx (mobile Tabs / desktop flex split)"
  key_links:
    - "results-layout.tsx -> MapViewWrapper -> dynamic(import map-view, {ssr:false}) -> MapView"
    - "MapView <- daysWithCoords built by joining optimizeResult.days[].visits to resolvedPlaces Map by placeId"
    - "AdvancedMarker onClick -> useCallback -> InfoWindow (prevents #404 infinite re-render)"
    - "APIProvider apiKey={process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY}"
---

<objective>
The final Phase 3 slice: the interactive map (DISP-03) and the responsive results layout (DISP-04). Implements the coordinate-gap join (optimize result carries no lat/lng, so visits are joined to resolvedPlaces by placeId), per-day colored polylines, numbered AdvancedMarkers, click-to-open InfoWindows that mirror the hoursUnknown warning, a DayLegend, the mandatory ssr:false dynamic-import wrapper, and the mobile-Tabs / desktop-flex ResultsLayout that keeps 375px scroll-free.

Purpose: Complete the map and mobile requirements, the last two of the five Phase 3 requirements. Replaces the 03-02 ResultsLayout placeholder with the real responsive container.
Output: MapView, MapViewWrapper, responsive ResultsLayout, and a passing DISP-03 map test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-ui/03-CONTEXT.md
@.planning/phases/03-core-ui/03-UI-SPEC.md
@.planning/phases/03-core-ui/03-RESEARCH.md
@src/lib/optimizer/schedule.ts
@src/types/itinerary.ts
@src/lib/map/day-colors.ts
@src/components/map-view.test.tsx
@src/components/results-layout.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MapView + MapViewWrapper (coordinate join, polylines, markers, InfoWindow) — turns map-view.test.tsx GREEN</name>
  <read_first>
    - src/components/map-view.test.tsx (RED DISP-03 test: vi.mock('@vis.gl/react-google-maps'); asserts one Polyline per day + one marker per visit)
    - .planning/phases/03-core-ui/03-RESEARCH.md Patterns 1, 2, 3 (MapView, MarkerWithInfoWindow + useCallback #404 fix, dynamic ssr:false) and Pitfalls 1, 2, 6, 7 (infinite loop, window-not-defined, lat/lng vs latitude/longitude, coordinate gap)
    - src/lib/optimizer/schedule.ts (visits have placeId but NO lat/lng)
    - src/types/itinerary.ts (PlaceCoord; ResolvedPlace carries lat/lng)
    - src/lib/map/day-colors.ts (getDayColor / DAY_COLORS)
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 4 (day palette, polyline strokeWeight 4 / opacity 0.85) + Flow E (marker + InfoWindow + DayLegend) + States table (map key missing placeholder)
  </read_first>
  <behavior>
    - Given days each with a coordinate-joined visit list, MapView renders exactly one Polyline per day (path = visit {lat,lng} in order, strokeColor = getDayColor(dayIndex), strokeWeight 4, strokeOpacity 0.85).
    - MapView renders exactly one AdvancedMarker per visit, content a circular day-colored badge showing the within-day visit number.
    - Clicking a marker toggles an InfoWindow showing displayName + "{scheduledStart} – {scheduledEnd}"; when that visit hoursUnknown is true the InfoWindow also shows "營業時間未知，建議出發前確認".
    - When NEXT_PUBLIC_GOOGLE_MAPS_API_KEY is empty, MapView renders the gray placeholder with text "地圖暫時無法顯示" instead of the map.
  </behavior>
  <action>
    Create `src/components/map-view.tsx` (`'use client'`) per 03-RESEARCH.md Patterns 1+2. Props: `days: Array<{ dayNumber: number; visits: Array<ScheduledVisit & PlaceCoord> }>` — i.e. coordinate-joined visits. Wrap in `<APIProvider apiKey={process.env.NEXT_PUBLIC_GOOGLE_MAPS_API_KEY!}>` (this exact env var — NOT NEXT_PUBLIC_MAPS_KEY). Inside `<Map mapId="itinerary-map" gestureHandling="greedy">`: render one `<Polyline>` per day (color via getDayColor(dayIndex), strokeWeight 4, strokeOpacity 0.85, path mapped to `{lat, lng}` — Pitfall 6) and one `MarkerWithInfoWindow` per visit. Implement `MarkerWithInfoWindow` with `useAdvancedMarkerRef()` and a `useCallback` click handler (MANDATORY — prevents #404 infinite loop) toggling an `InfoWindow` anchored to the marker; the InfoWindow shows displayName, time slot, and the amber "營業時間未知，建議出發前確認" line when hoursUnknown. Fit bounds to all markers on first load via `useMap()` + `useMapsLibrary('core')` LatLngBounds. Add an inline `DayLegend` positioned `absolute top-3 right-3` with a color pill per day ("第 N 天"). If the env key is missing/empty, short-circuit to the gray placeholder div with "地圖暫時無法顯示".
    Create `src/components/map-view-wrapper.tsx` (NO 'use client' — server component) exporting `MapView` via `dynamic(() => import('./map-view').then(m => m.MapView), { ssr: false, loading: () => <gray "地圖載入中..." div> })`. This is the only import path other components use (Pitfall 2 — never import map-view directly into SSR).
    Turn `map-view.test.tsx` green.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run src/components/map-view.test.tsx && test -f src/components/map-view-wrapper.tsx && grep -q 'ssr: false' src/components/map-view-wrapper.tsx && grep -q 'useCallback' src/components/map-view.tsx && grep -q 'NEXT_PUBLIC_GOOGLE_MAPS_API_KEY' src/components/map-view.tsx && grep -q '營業時間未知，建議出發前確認' src/components/map-view.tsx && echo OK</automated>
  </verify>
  <done>MapView renders one polyline per day + numbered markers per visit with click-to-open InfoWindows mirroring the hoursUnknown warning; map-view-wrapper uses dynamic ssr:false; key-missing path shows 地圖暫時無法顯示; map-view.test.tsx passes; tsc clean.</done>
</task>

<task type="auto">
  <name>Task 2: Responsive ResultsLayout — mobile Tabs / desktop flex split; wire MapView via coordinate join</name>
  <read_first>
    - src/components/results-layout.tsx (03-02 placeholder container — replace the map placeholder with real MapView via the wrapper)
    - src/components/place-input-panel.tsx (passes itinerary + resolvedPlaces + details Map into ResultsLayout)
    - src/components/itinerary-view.tsx (existing prop contract — do not break it)
    - .planning/phases/03-core-ui/03-RESEARCH.md Pattern 7 (mobile Tabs / desktop flex) + Code Examples (coordinate-join daysWithCoords builder)
    - .planning/phases/03-core-ui/03-UI-SPEC.md section 6 (layout dims) + section 10 (breakpoints) + section 9 (44px touch targets)
    - src/components/ui/{tabs,scroll-area}.tsx
  </read_first>
  <action>
    Build the coordinate join in `results-layout.tsx`: from `resolvedPlaces` create `coordMap = new Map(resolvedPlaces.map(p => [p.placeId, {lat: p.lat, lng: p.lng}]))`, then `daysWithCoords = itinerary.days.map(d => ({ dayNumber: d.dayNumber, visits: d.visits.map(v => ({ ...v, lat: coordMap.get(v.placeId)?.lat ?? 0, lng: coordMap.get(v.placeId)?.lng ?? 0 })) }))` (Pitfall 7). Pass `daysWithCoords` to `MapView` (imported from `./map-view-wrapper`, never directly from `./map-view`).
    Replace the 03-02 stacked placeholder with the responsive layout from 03-RESEARCH.md Pattern 7: mobile (`md:hidden`) a shadcn `Tabs` with triggers 行程表 / 地圖 (each `h-11` for touch targets), itinerary tab renders `ItineraryView`, map tab renders `MapView` in `h-[calc(100vh-200px)]`; desktop (`hidden md:flex gap-6`) renders `ItineraryView` in a `w-[420px] flex-shrink-0` `ScrollArea` beside `MapView` in `flex-1 sticky top-4 min-h-[600px]`. Container is `w-full max-w-full` so 375px never horizontally scrolls.
    Update `place-input-panel.tsx` only as needed to pass `resolvedPlaces` and the details Map into `ResultsLayout` (the slot reserved in 03-02/03-03). Keep all itinerary-side behavior from 03-03 intact.
    Ensure ResultsLayout receives optimizeResult (which carries hoursUnknown on each ScheduledVisit). MapView must receive the visits array directly from optimizeResult (not only from the details Map) so that InfoWindow can check `visit.hoursUnknown` even if the details fetch has not yet resolved. This ensures the hoursUnknown warning appears in the InfoWindow without depending on async details data.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && grep -q 'md:hidden' src/components/results-layout.tsx && grep -q '行程表' src/components/results-layout.tsx && grep -q 'map-view-wrapper' src/components/results-layout.tsx && grep -q 'w-\[420px\]' src/components/results-layout.tsx && echo OK</automated>
  </verify>
  <done>ResultsLayout shows mobile Tabs (行程表/地圖) and desktop side-by-side split; MapView gets coordinate-joined days; container is w-full max-w-full (no 375px horizontal scroll); full vitest suite green; tsc clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> Maps JS API | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY exposed in client bundle by design; Maps JS loads tiles/markers |
| resolvedPlaces -> MapView coordinate lookup | placeId-keyed join; missing ids fall back to (0,0) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-09 | Information Disclosure | NEXT_PUBLIC_GOOGLE_MAPS_API_KEY in bundle | medium | accept | Intentionally public for Maps JS; protection is GCP HTTP-referrer + Maps-JS-only scope (Phase 1 operator cost-controls task) |
| T-03-10 | Tampering | displayName in InfoWindow | low | mitigate | Rendered via React children (JSX-escaped); never dangerouslySetInnerHTML (03-RESEARCH.md Security Domain) |
| T-03-11 | Denial of Service | AdvancedMarker click handler | medium | mitigate | useCallback on the click handler prevents the #404 infinite re-render loop that locks the browser tab |
| T-03-12 | Tampering | unvalidated placeId in coordMap.get | low | mitigate | coordMap.get returns undefined for unknown ids; optional chaining `?? 0` already guards (no thrown access) |
</threat_model>

<verification>
- `npx tsc --noEmit` clean
- `npx vitest run` — full suite green including map-view.test.tsx (all 4 Wave-0 scaffolds now green across the phase)
- Manual: at 375px viewport the full flow has no horizontal scroll; Tabs toggle works; markers + polylines render per day; clicking a marker shows the InfoWindow with hoursUnknown warning
- map-view imported only via map-view-wrapper (ssr:false), never directly in SSR
</verification>

<success_criteria>
- DISP-03: per-day colored polylines + numbered markers + click InfoWindow with name/time
- hoursUnknown warning mirrored in the map InfoWindow (SC4 — both itinerary AND map)
- DISP-04: 375px usable, no horizontal scroll, mobile Tabs / desktop flex split
- Coordinate gap solved via placeId join from resolvedPlaces
</success_criteria>

## Artifacts this phase produces (this plan)

- `src/components/map-view.tsx` (+ green `map-view.test.tsx`), `src/components/map-view-wrapper.tsx`
- Responsive `src/components/results-layout.tsx`
- Updated `src/components/place-input-panel.tsx` (ResultsLayout wiring)

<output>
Create `.planning/phases/03-core-ui/03-04-SUMMARY.md` when done
</output>