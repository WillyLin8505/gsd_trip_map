---
phase: 06-interactive-day-editing
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - src/lib/optimizer/day.ts
  - src/lib/optimizer/day.test.ts
  - src/app/api/optimize/day/route.test.ts
  - src/components/day-card.tsx
  - src/components/place-input-panel.tsx
  - src/components/results-layout.tsx
  - src/components/itinerary-view.tsx
autonomous: false
requirements:
  - EDIT-02

must_haves:
  truths:
    - "Each day has an 「自動安排」 button that re-orders only that day by shortest path and re-times it; other days are untouched (D EDIT-02)."
    - "Auto-arrange classifies each place from Google place_types into 餐廳/點心/行程; only 餐廳 are slotted into lunch 11:30–13:30 or dinner 17:30–19:30; 點心 and 行程 are ordinary shortest-path route stops; a place matching both restaurant and snack types resolves to 餐廳; null/missing place_types → 行程 (no meal slot)."
    - "A failed F2 operation shows an inline message under that day and leaves the day exactly as it was — it is never wiped."
  artifacts:
    - src/lib/optimizer/day.ts
    - src/components/day-card.tsx
  key_links:
    - "A-submatrix index mapping: aMatrix[a][b] = matrix[A[a]][A[b]]; nearestNeighbor(aMatrix) returns indices 0..A.length-1 that MUST be mapped back via nnResult.map(i => A[i]) to full places[] indices before scheduleTimes (Pitfall 6)."
    - "reorder=true delegates all opening-hours correctness to scheduleTimes; the clock-walk pre-simulation uses ONLY visitDurationMinutes + matrix travel times (no opening-hours checks — Pitfall 8)."
    - "onAutoArrange is threaded place-input-panel → results-layout → itinerary-view → day-card; handleAutoArrange posts reorder:true to /api/optimize/day and calls replaceDay (no new route file — reorder is a body param)."
---

<objective>
## Phase Goal (MVP user story)

**As a** traveller refining a generated itinerary, **I want to** press a per-day 「自動安排」 button, **so that** that one day is re-ordered by shortest path with its restaurants slotted into lunch/dinner — without disturbing any other day.

## This plan (Slice F2 — EDIT-02)

Deliver the complete F2 vertical slice end-to-end by extending the F1 primitives from 06-01: add `classifyPlace` + the `scheduleSingleDay(reorder=true)` meal-slotting branch to `src/lib/optimizer/day.ts`, then wire the per-day 「自動安排」 button so a user can re-arrange any single day in the browser. The `POST /api/optimize/day` route already handles `reorder` as a body param (built in 06-01) — no new route file.

Purpose: Second user-reachable capability of Phase 6 — single-day auto-arrange with restaurant meal-slotting.
Output: extended `day.ts` (classifyPlace + reorder=true), the 「自動安排」 button in `DayCard`, `handleAutoArrange` + onAutoArrange threading.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-interactive-day-editing/06-CONTEXT.md
@.planning/phases/06-interactive-day-editing/06-RESEARCH.md
@.planning/phases/06-interactive-day-editing/06-PATTERNS.md
@.planning/phases/06-interactive-day-editing/06-UI-SPEC.md

# Built in 06-01 — read before extending
@src/lib/optimizer/day.ts
@src/lib/optimizer/day.test.ts
@src/app/api/optimize/day/route.ts
@src/app/api/optimize/day/route.test.ts
@src/components/place-input-panel.tsx
@src/components/results-layout.tsx
@src/components/itinerary-view.tsx

# Existing code to reuse / mirror
@src/lib/optimizer/route.ts
@src/lib/optimizer/schedule.ts
@src/components/day-card.tsx
</context>

<artifacts_produced>
New symbols introduced by this plan (exclude from source-drift verification):
- `classifyPlace` (+ private RESTAURANT_TYPES / SNACK_TYPES sets), `scheduleSingleDay` reorder=true branch — `src/lib/optimizer/day.ts`
- `onAutoArrange` prop on `DayCard` (+ per-day `isArranging`/`arrangeError` local state, 「自動安排」 button) — `src/components/day-card.tsx`
- `handleAutoArrange` handler (added to `place-input-panel.tsx`); `onAutoArrange` prop threaded through `results-layout.tsx` and `itinerary-view.tsx`
- Extended tests: reorder=true cases in `src/lib/optimizer/day.test.ts`; reorder=true route case in `src/app/api/optimize/day/route.test.ts`
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: classifyPlace + scheduleSingleDay(reorder=true) meal-slotting</name>
  <files>src/lib/optimizer/day.ts, src/lib/optimizer/day.test.ts</files>
  <read_first>
    - 06-CONTEXT.md §Place categories (3-type taxonomy, restaurant-wins, null→attraction), §Meal windows, §scheduleSingleDay reorder=true steps.
    - 06-RESEARCH.md §classifyPlace, §scheduleSingleDay reorder=true (step-by-step pseudocode), Pitfall 6 (A-submatrix mapping), Pitfall 8 (clock-walk uses only travel+duration), §nearestNeighbor/twoOptImprove signatures + matrix alignment contract.
    - 06-PATTERNS.md "src/lib/optimizer/day.ts" (submatrix extraction, classifyPlace), test fixtures (makePlace/makeMatrix/TEST_OPTS).
    - src/lib/optimizer/route.ts (nearestNeighbor, twoOptImprove — pure, operate on the submatrix), src/lib/optimizer/day.ts (the reorder=false branch + throw to replace).
  </read_first>
  <behavior>
    day.test.ts — extend the existing file with:
    classifyPlace:
    - returns "restaurant" for any of {restaurant, food, meal_takeaway, meal_delivery}.
    - returns "snack" for any of {cafe, bakery, dessert, ice_cream_shop, bar}.
    - returns "attraction" for null, [], and unknown types.
    - restaurant wins when a place has BOTH a restaurant type and a snack type.
    scheduleSingleDay reorder=true:
    - no restaurants → plain shortest-path over all stops (no meal slotting).
    - attractions are ordered by shortest path (NN + 2-opt over the A-submatrix), then mapped back to full indices.
    - one restaurant → it lands inside the lunch window (slot start 11:30–13:30).
    - two restaurants → one in lunch, one in dinner (17:30–19:30).
    - >2 restaurants → the extra restaurants are placed in route order like attractions.
    - place_types null is classified as attraction (no meal slot) — lock this explicitly.
  </behavior>
  <action>
    Extend day.test.ts FIRST (RED) with the classifyPlace and reorder=true cases above. Use the makePlace fixture but set place_types via the OptimizerPlace's source — NOTE: classifyPlace takes string[]|null directly; for scheduleSingleDay tests, place_types must reach the classifier, so read how the function accesses category (see below) and construct fixtures accordingly. Keep hoursUnknown:true on fixtures to avoid opening-hours rejections; use TEST_OPTS with a Monday travelDate.
    In src/lib/optimizer/day.ts: add the module-private const RESTAURANT_TYPES = new Set(["restaurant","food","meal_takeaway","meal_delivery"]) and SNACK_TYPES = new Set(["cafe","bakery","dessert","ice_cream_shop","bar"]). Export classifyPlace(placeTypes: string[] | null): "restaurant" | "snack" | "attraction" — null/empty → "attraction"; any restaurant-type match → "restaurant" (wins ties); else any snack-type match → "snack"; else "attraction". (Decide how classifyPlace reads types per place inside scheduleSingleDay: OptimizerPlace as defined in types.ts does not carry place_types — extend the function to accept the category via the place objects it is given. The route maps DB rows; thread place_types through to the scheduler by extending the OptimizerPlace objects the route builds with a placeTypes field, OR have the route pre-classify and pass a category. Choose the minimal approach and document it; the unit test must be able to drive classification.)
    Replace the reorder=true throw with the real branch per 06-RESEARCH.md: classify each place → R = restaurant indices, A = snack+attraction indices; build aMatrix[a][b] = matrix[A[a]][A[b]]; if A.length>1 run twoOptImprove(nearestNeighbor(aMatrix), aMatrix) else trivial; map orderedAFull = aOrder.map(i => A[i]) (Pitfall 6); clock-walk orderedAFull from opts.dailyStartMinutes using ONLY matrix travel + visitDurationMinutes (no opening-hours — Pitfall 8); on first entry into the lunch window (arrival in [690,810)) with rPool non-empty, insert the rPool restaurant nearest the current location, then continue; same for dinner ([1050,1170)); append any remaining restaurants in route order after the walk; if R is empty, finalOrder = orderedAFull. Then call scheduleTimes([finalOrder], places, matrix, opts) and return { day: { dayNumber: mode.dayNumber, visits: days[0]?.visits ?? [] }, unscheduled } — opening-hours correctness comes entirely from scheduleTimes.
  </action>
  <verify>
    <automated>npx vitest run src/lib/optimizer/day.test.ts</automated>
  </verify>
  <done>day.test.ts passes all classifyPlace + reorder=true cases; A-submatrix indices are mapped back via A[i] before scheduleTimes; clock-walk uses no opening-hours; null place_types classifies as attraction; restaurant wins ties; day.ts remains pure (no I/O).</done>
</task>

<task type="auto">
  <name>Task 2: 「自動安排」 button + onAutoArrange wiring (F2 end-to-end) + reorder=true route test</name>
  <files>src/app/api/optimize/day/route.test.ts, src/components/day-card.tsx, src/components/place-input-panel.tsx, src/components/results-layout.tsx, src/components/itinerary-view.tsx</files>
  <read_first>
    - 06-UI-SPEC.md §3 Flow F2 (states, loading, partial unscheduled, error), §4 placement (right-aligned in DayCard header, 44px touch), §5 copy, §6 a11y (per-day aria-label).
    - 06-PATTERNS.md "src/components/day-card.tsx" (onAutoArrange prop + button + local state), "src/components/place-input-panel.tsx" (handleAutoArrange, ResultsLayout call site threading).
    - 06-RESEARCH.md §Results Page State and Threading (threading chain: place-input-panel → ResultsLayout → ItineraryView → DayCard), open question 2 (per-day unscheduled display).
    - src/components/day-card.tsx (current header lines 33-44; already "use client", no state yet), src/components/results-layout.tsx, src/components/itinerary-view.tsx.
  </read_first>
  <action>
    Add a reorder=true happy-path case to src/app/api/optimize/day/route.test.ts: mocked scheduleSingleDay is called with mode { reorder:true, dayNumber } and the route returns 200 { day, unscheduled }.
    Modify src/components/day-card.tsx: extend DayCardProps with onAutoArrange?: (dayNumber: number) => void. Add local useState for isArranging and arrangeError. In CardHeader, replace the single 「共 N 個地點」 span with a flex group containing that span plus, when onAutoArrange is provided, a Button variant="outline" size="sm" className="h-11" aria-label={`自動安排第 ${day.dayNumber} 天`} that calls onAutoArrange(day.dayNumber); while in flight show 「安排中...」 + spinner and disable only this day's button (other days unaffected). On failure show an inline Alert variant="destructive" 「自動安排失敗，請稍後再試」 under the day; never clear the day. If the returned day has unscheduled places, surface them inline under that DayCard (reuse the UnscheduledAlert pattern or an inline Alert listing displayName + reason) while still rendering the scheduled stops (Claude's discretion per 06-RESEARCH open question 2).
    Modify src/components/place-input-panel.tsx: add async handleAutoArrange(dayNumber) — find that day in optimizeResult, collect placeIds from its visits, POST /api/optimize/day { placeIds, reorder:true, dayNumber, ...(startDate ? { travelDate:startDate } : {}) }; on !res.ok let DayCard show its own error (or surface via a thrown error the button catches); on success call replaceDay(dayNumber, day) (reusing the replaceDay added in 06-01, preserving suggestedDays). Pass onAutoArrange down through ResultsLayout.
    Modify src/components/results-layout.tsx and src/components/itinerary-view.tsx to forward onAutoArrange from place-input-panel → ResultsLayout → ItineraryView → each DayCard (mirror the existing onDurationChange threading; no visual change to the map column).
    Run tsc to confirm the prop chain types correctly.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
    <automated>npx vitest run</automated>
  </verify>
  <done>tsc passes; full suite green (incl. new reorder=true route case); each DayCard header shows 「自動安排」; clicking it re-orders only that day (restaurants in meal windows), other days untouched; per-day loading/error are local; any failure keeps that day intact; unscheduled places (if any) are surfaced inline.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verify — F2 auto-arrange walkthrough</name>
  <action>Blocking human-verification gate. Claude has automated everything; the human runs the steps in how-to-verify in the browser and approves or reports issues. No code is written in this task.</action>
  <what-built>F2 end-to-end: each DayCard has a 「自動安排」 button that re-orders only that day by shortest path and slots 餐廳 into lunch (11:30–13:30) / dinner (17:30–19:30) windows; 點心 and 行程 stay ordinary route stops. Other days untouched. Failures show an inline message and never wipe the day.</what-built>
  <how-to-verify>
    1. Generate a multi-day itinerary that includes at least one day with ≥1 restaurant (餐廳) and some attractions/snacks.
    2. Click 「自動安排」 on that day. Confirm only that day re-orders (shortest path), the restaurant(s) land in the 11:30–13:30 and/or 17:30–19:30 windows, 點心/行程 sit as ordinary stops, and other days are unchanged. The map polyline/marker order for that day updates.
    3. Click 「自動安排」 on a day with 0 restaurants. Confirm it succeeds as plain shortest-path with no meal slotting and no error.
    4. (If reproducible) induce a failure and confirm the inline 「自動安排失敗，請稍後再試」 appears and the day is left intact.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/optimize/day (reorder:true) | Untrusted JSON crosses into the server route (same route as 06-01) |
| route → Google Routes API | Server-side computeRouteMatrix; API key server-only |
| route → Postgres | placeIds parameterised inArray query; place_types read from DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-05 | Denial of Service | POST /api/optimize/day reorder=true (each 自動安排 triggers computeRouteMatrix) | low | accept | placeIds capped at 25 by optimizeDayRequestSchema (06-01); one matrix call per click; no extra control for v1 |
| T-06-06 | Tampering | place_types-driven classification | low | mitigate | classifyPlace is a pure allowlist lookup over fixed Sets; null/unknown safely default to attraction (no privileged behaviour) |
| T-06-02 | Information Disclosure | GOOGLE_PLACES_API_KEY | high | mitigate | Key read only from process.env server-side; never reaches the client (DayCard calls our own route only) |
</threat_model>

<verification>
- `npx vitest run` — full suite green (extended day.test.ts + route reorder=true case).
- `npx tsc --noEmit` — no type errors.
- Manual F2 walkthrough checkpoint approved.
</verification>

<success_criteria>
- classifyPlace maps place_types into 餐廳/點心/行程 correctly (restaurant wins ties; null → attraction).
- scheduleSingleDay(reorder=true) orders attractions by shortest path and slots restaurants into lunch/dinner windows; A-submatrix indices mapped back correctly.
- A user can auto-arrange any single day end-to-end; other days untouched; failures keep the day intact.
</success_criteria>

<output>
Create `.planning/phases/06-interactive-day-editing/06-02-SUMMARY.md` when done.
</output>
