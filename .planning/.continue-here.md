---
context: default
phase: post-v1 feature-extensions
status: paused
last_updated: 2026-06-26T15:50:35Z
---

# Continue Here — paused at ~78% context

GSD was just installed mid-session. New work should now go through GSD
(`gsd-phase` → `gsd-discuss-phase` → `gsd-plan-phase` → `gsd-execute-phase`)
rather than the superpowers flow used earlier today.

<current_state>
v1 (Phases 01–05) is code-complete and **deployed on Cloudflare** (live:
https://gsd-trip-map.sssss971412.workers.dev), auto-deploying via Workers Builds
on push to `main`. Two feature extensions were designed this session via
superpowers; **Part A is shipped**, **sub-project C is spec'd but not built**,
**Part B is only conceptual**. Branch `main`, everything pushed.
</current_state>

<completed_work>
- Phases 01–05: foundation/API, optimizer, core UI, auth+save+share, polish (duration override, rate limit, debounce).
- Cloudflare deploy: OpenNext + `pg`/pg-cloudflare + Hyperdrive (per-request `getDb()` via React `cache()` + `maxUses:1`; `serverExternalPackages`); IPv4 session pooler; Linux/npm10 lockfile; Workers Builds CI.
- **Part A** (input redesign + AI city): city optional, Claude Haiku inference (fail-open), 待幾天/開始日期 UI, confirm-page shows AI-detected city. Merged to main, deployed. Spec+plan in `docs/superpowers/`.
- **Sub-project C spec** written (`docs/superpowers/specs/2026-06-26-interactive-day-editing-design.md`, commit `ada233b`).
</completed_work>

<remaining_work>
1. **Sub-project C spec update**: fold in the 3-category taxonomy — 餐廳/點心/行程, auto-classified from Google `place_types`. Only 餐廳 → lunch (11:30–13:30) / dinner (17:30–19:30) slots; 點心 + 行程 → shortest-path route stops.
2. **Plan + build sub-project C** (via GSD): `scheduleSingleDay` (pure, reorder on/off + meals) → `POST /api/optimize/day` → results-page F1 (paste new place → add to geographically closest day) + F2 (per-day 自動安排 button).
3. **Part B**: AI place/itinerary recommendations from a user-configured source URL + admin backend. Needs its own brainstorm/spec.
</remaining_work>

<decisions_made>
- Cloudflare over Vercel (Vercel quota hit); postgres-js hangs on Workers → `pg`; module-level DB client hangs → per-request `cache()` + `maxUses:1`.
- DATABASE_URL = Supabase IPv4 **session pooler** (direct host is IPv6-only).
- Lockfile MUST be Linux/npm10-generated (`npm ci` on Cloudflare).
- Part A: `buildCityBias` returns null for unknown cities (50km radius cap fix); city folded into text query.
- Sub-project C: snacks (點心) behave as normal short route stops; only restaurants get meal slots.
</decisions_made>

<blockers>
None blocking. Operator/runtime items pending (non-blocking): set `ANTHROPIC_API_KEY` Worker secret + `.env.local`; GCP cost controls; 375px mobile UAT; Supabase Auth redirect URL for the live domain.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured state for `/gsd-resume-work`.
2. `docs/superpowers/specs/2026-06-26-interactive-day-editing-design.md` — sub-project C design (update for 3 categories before planning).
3. `.planning/STATE.md` — phase history.

## Infrastructure State
- Live Worker `gsd-trip-map` on Cloudflare; Hyperdrive `ff759a8e…` → Supabase pooler; secret `GOOGLE_PLACES_API_KEY` set; `ANTHROPIC_API_KEY` NOT yet set.
- Local dev: `node node_modules/next/dist/bin/next dev -p 4000` (NOT `npm run dev` — no win32 `next.cmd` shim). vitest works on Windows now.
- Cloudflare build/deploy: WSL (`~/gsd-deploy`, Node 22, reuses Windows wrangler login) OR Workers Builds CI (push to main).

<context>
Earlier session used superpowers (brainstorming/writing-plans/executing-plans); now GSD is installed — switch to GSD commands for the remaining work. The sub-project C spec is solid but predates the 3-category clarification; update it first.
</context>

<next_action>
Start with: update the sub-project C spec for the 3-category (餐廳/點心/行程) classifier, then run `gsd-plan-phase` for it (or `gsd-phase` to add it to ROADMAP first).
</next_action>
